
document.addEventListener('DOMContentLoaded',()=>{const r=document.querySelectorAll('.reveal');const o=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('show')})},{threshold:.12});r.forEach(el=>o.observe(el));const s=document.querySelector('[data-supply-target]');if(s){const end=Number(s.dataset.supplyTarget||0);let cur=0;const step=Math.max(1,Math.floor(end/88));const fmt=new Intl.NumberFormat('en-US');const t=setInterval(()=>{cur+=step;if(cur>=end){cur=end;clearInterval(t)}s.textContent=fmt.format(cur)+' WIN'},18)}const bars=document.querySelectorAll('.bar-fill');const bo=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting)x.target.style.width=(x.target.dataset.value||0)+'%'})},{threshold:.3});bars.forEach(b=>bo.observe(b))});
